image: node:14
pages: 
  cache: 
    paths:
      - .next/
      - .npm/ 
  script:
    - npm ci --cache .npm 
    - npm run export
    - rm -rf public
    - mkdir -p public
    - cp -r out/* public
  artifacts:
    paths:
      - public
  only:
    - master